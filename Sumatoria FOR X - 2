#include <stdio.h>

int main(){
    int n, i, x;
    
    scanf("%d", &n);
    
    if(n <= 0){
        printf("Entrada no válida");
        return 0;
    }
    
    for( i=1 ; i <= n ; i++){
        x+= 2*i;
    }
    
    printf("%d\n", x);
    return 0;
}
