#include <stdio.h>

int main(){
    int n, i, x = 0;
    
    scanf("%d", &n);
    
    if(n <= 0){
        printf("Entrada no válida\n");
        return 0;
    }
    
    for( i = 1; i <= n; i++){
        x += i;
    }
    
    printf("%d\n", x);
    
    return 0;
}
